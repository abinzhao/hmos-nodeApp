# 应用市场服务后台

## 1. 项目结构

```
.
├── config
│   └── default.json // 配置文件
├── controllers
│   └── commandController.js // 命令控制器
├── routes
│   └── api.js // API路由定义
├── services
│   └── tcpService.js // TCP服务逻辑
├── .env // 环境变量配置
├── app.js // Express应用入口
└── server.js // 启动脚本
```
